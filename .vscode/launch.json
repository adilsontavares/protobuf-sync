{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Server",
            "type": "php",
            "request": "launch",
            "program": "${workspaceRoot}/src/server.php",
            "cwd": "${fileDirname}",
            "port": 9000
        }
    ]
}